{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Debug Quasar Frontend",
            "url": "http://localhost:9000/#/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}